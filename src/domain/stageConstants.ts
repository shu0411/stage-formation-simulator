/** ステージ寸法とグリッド間隔（1.6 ステージ / グリッド表示）。2D・3D の両方がこの定数を参照する。 */
export const STAGE_WIDTH = 12;
export const STAGE_DEPTH = 8;
export const STAGE_HALF_WIDTH = STAGE_WIDTH / 2;
export const STAGE_HALF_DEPTH = STAGE_DEPTH / 2;
export const GRID_INTERVAL = 1;
